export default class LetterPosition{
    position: number;
    letter: string;

    constructor(position: number, letter: string) {
        this.position = position;
        this.letter = letter;
    }
}